deployment:
  master:
    branch: master
    commands:
      - git config user.email "builds@circleci.com"
      - git config user.name "CircleCI Buil Agent"
      - ssh -t dev.usrz.com /bin/bash -c '"cd /var/www/libraries ; git pull"'
